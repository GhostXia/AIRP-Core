# AIRP-Core 环境变量示例（复制为 .env 后填写）

# 上游 LLM 端点（OpenAI 兼容格式）
AIRP_ENDPOINT=https://api.openai.com/v1/chat/completions

# 上游 API Key（必填）
AIRP_API_KEY=sk-your-api-key-here

# 默认模型
AIRP_MODEL=gpt-4o

# daemon 访问 Key（留空 = 不启用鉴权；公网部署请务必设置）
AIRP_ACCESS_KEY=

# 日志级别
AIRP_LOG=info
